<div class="row">
  <div class="col-lg-12">
    <div class="container">
      <%= link_to :back do %>
        <p class="text-muted"><i class="fa fa-arrow-left"></i> Back </p>
      <% end %>
      <span class="text-primary"><%= @page.content(:date).try(:html_safe) %></span>
      <h1> <%= @page.title %> </h1>
      <%= @page.content(:text).try(:html_safe) %>
    </div>
  </div>
</div>
